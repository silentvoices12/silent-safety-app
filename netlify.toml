[build]
  command = "npm run build"
  publish = ".next"

[[plugins]]
  package = "@netlify/plugin-nextjs"
```

Save with **Ctrl + S** then run:
```
npm install @netlify/plugin-nextjs --save-dev
```

Then:
```
git add . ; git commit -m "fix netlify routing" ; git push